<!--
  Copyright (C) 2020, exense GmbH

  This file is part of STEP

  STEP is free software: you can redistribute it and/or modify
  it under the terms of the GNU Affero General Public License as published by
  the Free Software Foundation, either version 3 of the License, or
  (at your option) any later version.

  STEP is distributed in the hope that it will be useful,
  but WITHOUT ANY WARRANTY; without even the implied warranty of
  MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
  GNU Affero General Public License for more details.

  You should have received a copy of the GNU Affero General Public License
  along with STEP.  If not, see <http://www.gnu.org/licenses/>.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <artifactId>step-agent-provisioning-local</artifactId>
  <name>${project.groupId}:${project.artifactId}</name>

  <parent>
    <groupId>ch.exense.step</groupId>
    <artifactId>step</artifactId>
    <version>0.0.0-SNAPSHOT</version>
  </parent>

  <dependencies>
    <dependency>
      <groupId>ch.exense.commons</groupId>
      <artifactId>exense-basic-commons</artifactId>
    </dependency>
    <!--
      The embedded grid the started agents register to. Deliberately NOT depending on step-grid-agent: the agents run
      as separate processes out of their own, isolated set of libraries, which is what keeps keywords away from the
      classpath of whoever provisions them.
    -->
    <dependency>
      <groupId>ch.exense.step</groupId>
      <artifactId>step-grid-server</artifactId>
    </dependency>
    <dependency>
      <groupId>ch.exense.step</groupId>
      <artifactId>step-grid-client</artifactId>
    </dependency>
    <!-- Agent provisioning driver SPI and the token forecasting the local driver is driven by -->
    <dependency>
      <groupId>ch.exense.step</groupId>
      <artifactId>step-plans-base-artefacts</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.dataformat</groupId>
      <artifactId>jackson-dataformat-yaml</artifactId>
    </dependency>
    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <!--
            The Java agent this module starts is provided by whoever ships it, as a resource named after
            JavaLocalAgentProvider.EMBEDDED_AGENT_RESOURCE - the CLI embeds it to be self-contained. This module does
            not embed it: it is ~22 MB, and a consumer running next to an existing agent installation has no use for
            it. The tests do start the real agent, hence this copy into the test classes, which never reaches the
            published jar.
          -->
          <execution>
            <id>copy-local-java-agent-for-tests</id>
            <phase>generate-test-resources</phase>
            <goals>
              <goal>copy</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>ch.exense.step</groupId>
                  <artifactId>step-agent</artifactId>
                  <version>${project.version}</version>
                  <classifier>jar-with-dependencies</classifier>
                  <type>jar</type>
                  <outputDirectory>${project.build.testOutputDirectory}</outputDirectory>
                  <destFileName>step-local-java-agent.jar</destFileName>
                </artifactItem>
              </artifactItems>
              <overWriteReleases>true</overWriteReleases>
              <overWriteSnapshots>true</overWriteSnapshots>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
