package step.plugins.timeseries.dashboards.model;

public enum AxesColorizationType {
    
    STROKE, // only the stroke of each series is colorized
    FILL // each series is filled with color
    
}
